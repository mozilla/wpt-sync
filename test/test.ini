[paths]
repos = repos
worktrees = work
logs = logs

#[pulse]

[database]
url=sqlite:///sync.db
echo=true

[sync]
integration.autoland = repos/remotes/gecko
integration.mozilla-inbound = repos/remotes/gecko
landing = repos/remotes/gecko

[gecko]
repo.remote.mozilla = hg::repos/remotes/gecko
# Might be better to ensure these are configured, but for test this is easy
refs.central = mozilla/central
refs.mozilla-inbound = mozilla/inbound
refs.autoland = mozilla/autoland
path.wpt = testing/web-platform/tests

[web-platform-tests]
repo.url = repos/remotes/web-platform-tests
repo.remote.origin = repos/remotes/web-platform-tests

# [bugzilla]